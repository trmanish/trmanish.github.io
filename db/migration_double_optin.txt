-- Run this in Neon SQL editor to add double opt-in columns

ALTER TABLE subscribers ADD COLUMN IF NOT EXISTS confirmation_token TEXT UNIQUE;
ALTER TABLE subscribers ADD COLUMN IF NOT EXISTS confirmed BOOLEAN DEFAULT FALSE;

-- Grant app_subscriber permission to update the confirmed status
GRANT UPDATE ON subscribers TO app_subscriber;
